/**
 * @infra/adapters — Dynamic multi-database adapters (PostgreSQL / LibSQL), connection resolver, pool cache and health checks.
 * Phase 1 · T1.1 scaffold. Implementation lands in later tasks (see process.md).
 */
export const PACKAGE_NAME = '@infra/adapters' as const;
