/**
 * @infra/auth — Better Auth instance with OAuth providers and app-scoping plugin.
 * Phase 1 · T1.1 scaffold. Implementation lands in later tasks (see process.md).
 */
export const PACKAGE_NAME = '@infra/auth' as const;
